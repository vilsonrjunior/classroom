<p id="notice"><%= notice %></p>


<% if current_teacher.admin? %>
<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th>Student number</th>
      <th>First name</th>
      <th>Last name</th>
      <th>Age</th>
      <th>Level</th>
      <th>Course</th>
      <th><%=Teacher %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.id %></td>
        <td><%= student.first_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.age %></td>
        <td><%= student.level %></td>
        <td><% if student.course != nil %></td>
        <td><%= student.course.name %></td>
        <% else %>
        <td>No course</td>
        <% end %>
        <% if student.course != nil %>
        <td><%= student.course.teacher.first_name%></td>
        <% else %>
        <td>No teacher</td>
  <% end %>
</p>
        <td><%= link_to 'Show', student %></td>
        <% if current_teacher.admin? %>
        <td><%= link_to 'Edit', edit_student_path(student) %></td>
        <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
        <% end %>
      </tr>
  </tbody>
</table>

<br>

<% if current_teacher.admin? %>
<%= link_to 'New Student', new_student_path %>
<% end %>
<% else %>
<h2>Your students</h2>
<% current_teacher.students.each do |student|  %>
<ul><%=student.first_name  %></ul>
<% end %>
<% end %>

