<%= simple_form_for(@student) do |f| %>
  <%= f.error_notification %>
  <% DateTime.now.strftime("%d/%m/%Y - %H:%M %p") %>
  <div class="row">
    <div class="col-xs-12 col-md-6">
      <%= f.input :first_name %>
      <%= f.input :last_name %>
      <%= f.input :parent_id, collection: Parent.all %>
      <%= f.input :email %>
      <%= f.input :password %>
      <%= f.input :age, as: :date, start_year: Date.today.year - 15,
                            end_year: Date.today.year - 12, discard_day: false,
                              order: [:day, :month, :year] %>
      <%= f.input :course_id, collection: Course.all %>
    </div>
  </div>
  <%= f.button :submit, class: 'btn btn-primary btn-fill' %>
<% end %>
