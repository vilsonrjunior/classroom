<!-- <h2>Our Teachers</h2>

<div>
  <form action="/teachers" method="get">
    <input type="text" name="first_name" placeholder="Please type name or languages">
    <input type="submit">
  </form>
</div>

<div>
  <p><%= link_to "Create new teacher", new_teacher_path %></p>
</div>

<div>
<ul>
  <% @teachers.each do |teacher| %>
   <li>

    <p><%= link_to teacher.first_name, teacher_path(teacher) %></p>

    Teaches <%= teacher.language %>

    Contact email is <%= teacher.email %>

   </li>
  <% end %>
</ul>
</div>
 -->

<p id="notice"><%= notice %></p>

<h1>Teachers</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Language</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><%= teacher.id %></td>
        <td><%= teacher.first_name %></td>
        <td><%= teacher.last_name %></td>
        <td><%= teacher.language %></td>
        <td><%= teacher.email %></td>
        <td><%= link_to 'Show', teacher %></td>
        <td><%= link_to 'Edit', edit_teacher_path(teacher) %></td>
        <td><%= link_to 'Destroy', teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Teacher', new_teacher_path %>
